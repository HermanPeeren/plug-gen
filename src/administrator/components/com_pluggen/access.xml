<?xml version="1.0" encoding="utf-8"?>
<access component="com_pluggen">
	<section name="component">
		<action name="core.admin" title="JACTION_ADMIN"/>
		<action name="core.options" title="JACTION_OPTIONS"/>
		<action name="core.manage" title="JACTION_MANAGE"/>
		<action name="core.create" title="JACTION_CREATE"/>
		<action name="core.delete" title="JACTION_DELETE"/>
		<action name="core.edit" title="JACTION_EDIT"/>
		<action name="core.edit.state" title="JACTION_EDITSTATE"/>
		<!--
			Generating writes executable PHP. Anyone allowed to do it can run code
			on the server once the result is installed, so this is deliberately a
			separate action and should be treated as Super User equivalent.
		-->
		<action name="core.generate" title="COM_PLUGGEN_ACTION_GENERATE"
			description="COM_PLUGGEN_ACTION_GENERATE_DESC"/>
	</section>
</access>
